{
image_topic: "/camera/color/image_raw_crop",        # Set the image topic

camera_tf: "camera_link",              # Set the camera frame

object_name: "box1",                       # Set the object to be detected

reset_bb: True,                            # Set the 2d bounding box manually

dept_topic: "/camera/depth/image_rect_raw",  # Set the depth image topic

dept_info_topic: "/camera/depth/camera_info", # Set the depth camera info topic
}