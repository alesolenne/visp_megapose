{
image_topic: "/camera/color/image_raw_crop",        # Set the image topic

camera_info_topic: "/camera/color/camera_info_crop", # Set the camera info topic

camera_tf: "/camera_color_optical_frame",              # Set the camera frame

object_name: "box1",                       # Set the object to be detected

reinitThreshold: 0.1,                      # Reinit threshold for init and track service

reset_bb: False                            # Set the 2d bounding box manually
}